export const config = {
  baseUrl: process.env.BASE_URL || 'http://localhost:8081/',
  adminEmail: process.env.ADMIN_EMAIL || 'test@liferay.com',
  adminPassword: process.env.ADMIN_PASSWORD || 'learn',
};